# Banco de dados PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=

# JWT (obrigatório para login e registro)
JWT_SECRET=

# Discord — webhook para notificações de tickets (opcional)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# CORS (opcional) — lista de origens separadas por vírgula
# CORS_ORIGINS=http://localhost:5173,https://seu-dominio.com

# Porta do servidor (opcional; o backend usa 3000 por padrão no código)
# PORT=3000
