services:
  backend:
    build: ./backend
    env_file: ./backend/.env
    environment:
      NODE_ENV: development
    ports:
      - "3000:3000"
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    restart: unless-stopped
